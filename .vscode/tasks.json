{
    "version": "2.0.0",
    "tasks": [
      {
        "label": "build active file",
        "type": "shell",
        "command": "/usr/bin/clang++",
        "args": [
          "-std=c++17",
          "${file}",
          "-o",
          "${fileDirname}/${fileBasenameNoExtension}"
        ],
        "group": {
          "kind": "build",
          "isDefault": true
        },
        "problemMatcher": ["$gcc"],
        "detail": "Build active C++ file using clang++"
      }
    ]
  }